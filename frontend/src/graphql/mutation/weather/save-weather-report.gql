mutation SaveWeatherReport($input: WeatherReportInput!) {
  saveWeatherReport(input: $input) {
    success
    message
    report {
      id
      timestamp
      temperature
      pressure
      humidity
      cloudCover
    }
  }
}
